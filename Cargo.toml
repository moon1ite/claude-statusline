[package]
name = "claude-status"
version = "0.1.2"
edition = "2021"
description = "Parse Claude Code transcript for statusline display"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
opt-level = 3
lto = true
strip = true
